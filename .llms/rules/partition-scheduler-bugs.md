# Partition Scheduler Known Issues & Patterns

> **For full architectural context**, load the `partition-scheduler` skill which points to the design docs (PartitionSchedulingMeta.md, BufferAllocation.md, etc).

> Update this file when an issue is triaged/fixed and PartitionSchedulingMeta.md if necessary

## Code Location
- Partition assignment: `third_party/nvidia/hopper/lib/Transforms/WarpSpecialization/PartitionSchedulingMeta.cpp`
- Buffer allocation: `WSCodePartition.cpp` → `doBufferAllocation()` → `createLocalAlloc()`
- Code partition: `WSCodePartition.cpp` → `doCodePartition()`

## Debugging Regression between directory A and B
- If IR dumps are provided after each pass:
  - Find the IR right before partition scheduler for the right kernel, and save as file
- Do not guess, run triton-opt for the partition scheduler pass with debugging enabled or add debugging when needed, to check what happened at each phase (phases are defined in the PartitionSchedulingMeta.md)
- Run directory A's triton-opt on A's IR dump, and run directory B's triton-opt on B's IR dump, and compare
- Show the differences and figure out which phase caused the issue
- **Important**: Check BOTH directories for the same kernel. MetaMain at `~/local/MetaMain/triton/t.dump` may have both fwd and bwd kernels.

## Known Bugs & Fixes

### 1. getIntOrFloatBitWidth crash on pointer-typed 1D tensors (2026-04-14)
- **Symptom**: `Assertion 'isIntOrFloat()' failed` in `doBufferAllocation`
- **Manifestation**: We hit this when trying to create a 1D channel for pointer tensor. In general, partition scheduler should not put produer and consumer associated with pointer tensor in different partitions. So we will not have a need for a channel that is a pointer tensor. The root cause is in PSM.

### 2. Shared memory overflow from alpha cross-partition channel (2026-04-14, fixed)
- **Symptom**: `OutOfResources: shared memory, Required: 232712, Hardware limit: 232448` in FA forward persistent with dp=2
- **Manifestation**: After rebasing to upstream Triton, `TritonGPURemoveLayoutConversions` chose `#linear` layout instead of `#blocked` for the accumulator. This inserted a `ConvertLayoutOp` between `ExpandDimsOp` and `BroadcastOp` in the alpha correction chain.
- **Fix applied**: Added `cloneOperandChain` in `optimizeSchedule` that walks backward from a cloned `BroadcastOp`/`ExpandDimsOp` and also clones any `ConvertLayoutOp`/`BroadcastOp`/`ExpandDimsOp` feeding it from a different partition.
- **Commit**: `67af25ea`

### 3. optimizeSchedule too broad / too narrow for Blackwell vs Hopper (2026-04-17, fixed)
- **Symptom (Blackwell)**: `channels sharing the same producer must be in the same task` assertion in `WSCodePartition.cpp:createBuffer` when using the broad `isPure(op)` filter.
- **Symptom (Hopper)**: `producerTaskIds.size() == 1` assertion in `CodePartitionUtility.cpp:createChannelPost` when using a restrictive filter that excludes `MemDescTransOp`.
- **Root cause**: The `optimizeSchedule` op filter must be selective:
  - Too broad (any pure single-result op): cascading cloning of expensive ops (`tt.reduce`, `arith.mulf`, etc.) into computation partitions on Blackwell, violating channel invariants.
  - Too narrow (only `ConvertLayoutOp/BroadcastOp/ExpandDimsOp`): `memdesc_trans` shared by two `warp_group_dot` ops in different partitions on Hopper doesn't get cloned, creating a cross-partition memdesc dependency WS can't handle.
- **Fix**: Added `MemDescTransOp` to the allowed op list: `isa<MemDescTransOp, ConvertLayoutOp, BroadcastOp, ExpandDimsOp>(op)`. `MemDescTransOp` is metadata-only (reinterprets shared memory layout) so it's safe and cheap to clone.
- **Lit test**: `partition-scheduling-meta-hopper-fa.mlir` checks for two `memdesc_trans` copies with different partitions.

### 4. Non-deterministic epilogue partition assignment from DenseMap iteration (2026-04-17, fixed)
- **Symptom**: `producerTaskIds.size() == 1` assertion — `math.log2` for dp1's result gets partition 2 (dp0's) instead of partition 1, creating a cross-partition dependency with its downstream `arith.addf` in partition 1.
- **Root cause**: Two issues:
  1. Yield operands for `l_i` (softmax sum) and similar non-MMA-feeding ops are NOT in `opToDpId` (they're not in any MMA's backward slice). The post-loop dpId assignment at lines 576-578 skips these results.
  2. The fallback `dpIdToPartition.begin()->second` in `getEpilogueTarget` uses `DenseMap` iteration, which is non-deterministic across builds. Different binaries pick different partitions.
- **Fix**:
  1. Added `findDpIdBackward` helper that walks backward from a yield def through its operand chain to find an ancestor in `opToDpId` (e.g., finds `alpha_exp` which has the correct dpId).
  2. Replaced `dpIdToPartition.begin()->second` with `std::min_element` on the key for deterministic fallback.
- **Lit test**: `partition-scheduling-meta-hopper-fa.mlir` checks that `tt.expand_dims` on `#1` (dp0) gets partition 2 and `#4` (dp1) gets partition 1.

### 5. BWD softmax chain assigned to reduction instead of computation (2026-04-18, fixed)
- **Symptom**: In BWD FA with TMA descriptor_load for m/Di values, the pT chain (`convert_layout → expand_dims → broadcast → arith.subf → math.exp2 → arith.truncf → tmem_alloc`) gets partition 0 (reduction) instead of partition 3 (computation).
- **Root cause**: The load-user scheduling (Phase 4) walks forward from every categorized `descriptor_load` and assigns all transitive users to `defaultPartition`. For BWD, `defaultPartition` falls back to `reductionPartition` (partition 0) via `getDefaultPartition()` since no correction/epilogue/computation partition exists yet. When m/Di values come through `descriptor_load` (TMA), this walk transitively pulls the entire softmax chain into the reduction partition. The lit test used `tt.load` (pointer-based) for m/Di which is NOT categorized as a Load, so the issue was hidden.
- **Fix**: Added guard `defaultPartition != reductionPartition` to the load-user scheduling condition. When `defaultPartition` is just a fallback to reduction (BWD case), the load-user walk is skipped. Phase 5's MMA forward walk correctly assigns the softmax ops to computation instead.
- **Key insight**: The `loops` array in `getInitialSchedule` is ordered `[inner, outer]` (not `[outer, inner]`). Phase 5's `loops[0]` check matches inner-loop MMAs, so `scheduleUsers` DOES run on them. The issue was purely in Phase 4's load-user scheduling being too aggressive.

### 6. Forward set dead-ends at scf.yield inside causal-mask scf.if (2026-05-28, fixed)
- **Symptom**: TMEM exhaustion (`640 vs 512` limit) in causal FA w/ manual dp. The pass produces 8 partitions / 4 computation instead of the expected 6 / 2.
- **Root cause**: In `collectMMABackwardSlices`, the QK MMA's forward user set walk dead-ends at `scf.yield` (a terminator with no SSA results) when it enters the causal-mask `scf.if` region. The forward set is trapped inside the region and never reaches the softmax chain that the PV MMA's backward slice contains. As a result, union-find fails to merge the QK and PV MMAs, so they land in separate computation partitions.
- **Fix**: Make the forward walk follow `scf.yield` → the parent `scf.if`'s corresponding result, using the specific yield operand index so multiple data partitions (e.g., flex attention) are not merged. For multi-result `scf.if`, the walk follows one value at a time from the worklist. With the enlarged forward set, the QK MMA's forward set overlaps the PV MMA's backward slice and they share a compute partition.
- **Lit test**: `partition-scheduling-meta-causal-attention.mlir` (new) checks exactly 6 partitions / 2 computation (fails with 8 / 4 without the fix). Also removed a stale `CHECK-NOT` on `arith.mulf` scores ops in `partition-scheduling-meta-flex-attention.mlir`, which now correctly get computation partitions through the enlarged forward set.

### 7. Empty-producer assertion from partition-less shared `local_alloc` (2026-06-10, fixed)
- **Symptom**: `producerTaskIds.size() == 1` assertion in `CodePartitionUtility.cpp` `createChannelPost` (NDEBUG: `handleOperandD: expected exactly one producer task ID, got 0` + segfault).
- **Manifestation**: FA3 backward (`_attn_bwd_persist`, `cuda:100`, `data_partition_factor=2`). `separateLocalAllocWithSrc` splits a shared `local_alloc` that carries no `async_task_id` (hoisted above all partitions) into `local_alloc + local_store`; the store inherits the empty task-id set, so `producerTaskIds` is empty in `createChannelPost`.
- **Root cause**: `createChannelPost`'s `else` branch assumes exactly one producer task. An *empty* set (size 0) — not a multi-producer — is what trips the assert; the `size() > 1` branch already handles multiple producers.
- **Fix**: Guard `if (producerTaskIds.empty()) return;` — an alloc that belongs to no partition needs no cross-partition channel. Companion: `separateLocalAllocWithSrc` now tags the split `local_store` with the source op's single task ID (e.g. the TMA load) so a single-source shared buffer forms a clean 1-producer→N-consumer channel instead of one buffer per consumer.
- **Lit test**: `ws_code_partition_empty_producer_guard.mlir` runs `_attn_bwd_persist` through `--nvgpu-warp-specialization`; aborts without the guard, passes with it.

### 8. Cross-stage SMEM buffer silently downgraded to depth 1 (2026-06-22, fixed)
- **Symptom**: Runtime mbarrier **deadlock** in `_attn_bwd_persist` (FA backward, `ws_persistent`, `cuda:100`) with `early_tma_store_lowering=True` + `num_stages=2` + the heuristic `_BWD_DOT_ATTRS_SCHED` schedule (`bwd_config_idx=1`). Every warp-group spins on `q = desc_q.load` / dependent MMAs.
- **Root cause** (`WSMemoryPlanner.cpp`, `allocateSmemBuffers`): `q` (128×128×f16) is cross-stage — consumed by qkT MMA at `loop.stage=0` and dk MMA at `loop.stage=1` — so it needs `buffer.copy=2`. Phase 2 set `q`'s 2nd copy then budget-checked via `computeTotalSmem`, which counted the 3 early-TMA staging allocs (dq reduce + dk/dv store, ~32 KB) at full size, so `q`'s 2nd copy exceeded `smem_budget` (200000) and Phase 2 **silently reverted `q` to copy 1**. Phase 3.6 reuse — which would alias the staging SMEM and free the space — only runs `if (baseTotal > smemBudget)`, but the revert put the total back under budget, so reuse never fired. Chicken-and-egg: reverting the floor kept us under budget, so the space the floor needs was never reclaimed.
- **Fix**: Make the cross-stage minimum a strict, first-applied floor. (1) Phase 2 sets `minCopies = max(maxConsumerStage - minConsumerStage + 1, 1)` (capped at `num_buffers`) via new `getSmemCrossStageDepth`, applied unconditionally with **no budget revert**. (2) Phase 3.6 now reclaims **only discretionary** space — TMA-staging buffers — and excludes co-live operand buffers (new `isSmemLiveAcrossInnerLoop` guard) so `q`/`k`/`v` are never aliased. (3) If the floor still doesn't fit after staging reuse, ship it anyway (HW SMEM limit / `OutOfResources` at codegen is the backstop) rather than reverting. Phase 4/4.5 reverts/splits clamp to `minCopies`. This realigns the code with the documented design in `SmemAllocationDesign.md` ("no budget check in Phase 2").
- **Lit tests**: `ws_memory_planner_bwd_persist_early_tma.mlir` (new — real 128-wide failing IR: `q` now `buffer.copy=2`, store-staging reused). Updated `ws_memory_planner_bwd_persist.mlir` and `ws_memory_planner_bwd.mlir` (`q` 1→2, both cross-stage buffers get their floor; `v`/`k` not aliased). E2E: `fused_attention_ws_device_tma.py::test_op[1-False-0-False-triton-fp16-ws_persistent-bwd-False-128-1024-16-8]` now passes (was a hang); `0-…` config unregressed.

### 9. Persistent FA-bwd dv/dk staging SMEM cross-tile race (2026-06-26, fixed)
- **Symptom**: Wrong, **non-deterministic** dv/dk gradients (max-abs error ~3-4 vs ~3e-3) in `_attn_bwd_persist` (`ws_persistent`, `cuda:100`) with `early_tma_store_lowering=True` + `bwd_config_idx=2` (the `_BWD_DOT_ATTRS_TMEM` 3-group reuse config), `HEAD_DIM=128`. The non-persistent (`ws`) path with the same config and the TLX reference are both correct, and persistent idx 0/1/3/4 are correct. compute-sanitizer racecheck flags SMEM hazards on the v/do region (synccheck clean). The combination had **zero test coverage** (`test_op` skipped idx 2 on the persistent path).
- **Root cause** (`WSCodePartition.cpp`, "Step 7.5"): the dv/dk TMA-staging buffers alias the v/do operand SMEM (`allocation.reuseTarget`, realized by `mergeStagingReuseIntoHost`). Across the persistent outer tile loop the *next* tile's operand load can overwrite that SMEM while the *previous* tile's staging TMA store is still reading it. The existing guard was degenerate: a Step-7.5 `producer_acquire` emitted with constant `bufferIdx=0`/`phase=0`, and as a **same-task** channel whose consumer-release is dropped by `WSLowerToken`'s same-partition elision → a no-op. Harmless on the single-tile non-persistent path; a cross-tile SMEM race on persistent.
- **Fix**: Replace the degenerate guard with a dedicated single-buffered **cross-partition** reuse token (mirrors the TLX persistent `k_empties` pattern). The load task `producer_acquire`s it at the top of the persistent outer loop (before the operand loads); the staging task `consumer_release`s it at the bottom (after the staging stores have drained). A separate token avoids the `consumerWarps == nWarps` assert (MMA = 1 warp vs staging = 8 warps). The acquire phase is a 0-based iteration counter normalized as `(iv - lb)/step` (folds to the IV for the normalized persistent loop) so parity is correct regardless of loop bounds/step. The coarse token (acquire-before-all-loads / release-after-all-stores) covers every aliasing pair that shares one outer loop + load task + staging task; the pass verifies that invariant across all `stagingReuseInfos` and skips (with an `LDBG`) rather than emitting a barrier that would guard only one pair.
- **Tests**: Python regression `test_bwd_tmem_dsT_reuse_3group_persistent` (and `test_op` now exercises `ws_persistent`+idx2). E2E: ws_persistent+idx2 now correct (~3e-3) and deterministic (dk/dv run-variance 0); 48-run sweep worst 2.9e-3, 0 NaN. Full WarpSpecialization lit suite: 0 failures. Residual racecheck SMEM hazards are mbarrier false positives (the correct `_attn_fwd_persist` shows the same class).
- **Lit test**: `ws_code_partition_bwd_persist_staging_war.mlir` (real idx2 `_attn_bwd_persist` IR through `--nvgpu-test-ws-code-partition`) pins the Step 7.5 cross-tile WAR token: a single `1x!nvws.token` `producer_acquire`d by the load task (`dstTask=3`) at the top of the persistent outer loop with a loop-carried phase, and `consumer_release`d of the **same** token by the staging task (`dstTask=2`, constant index, region 4) at the bottom — i.e. one coarse cross-tile barrier, not per-iteration serialization (the staging is a `memdesc_reinterpret` of the `copy=1` v/do operand, so there is no second slot to pipeline into).

### 10. Early-TMA store-staging: wrong slot + unrealizable-reuse undercount (2026-06-26, fixed; T277224987)
- **Symptom**: Two distinct failures when the FA-bwd early-TMA dk/dv/dq store-staging is driven to depth 2 (e.g. `bwd_config_idx=0/1` at `SMEM_BUDGET=220000`, used as a repro). First, **wrong dv/dk/dq** (~74% mismatch). After fixing that, an **`OutOfResources` (Required 249348 > 232448)** that the planner's own budget model claimed fit (215040 ≤ 220000).
- **Root cause A — staging slot coupled to outer accumCnt** (`CodePartitionUtility.cpp`, `getStaggeredAccumCnt`): slot was `(accumCnt + theIdx) % numBuffers`. The TMA store-wait is a *fixed in-flight-count* wait (`cp.async.bulk.wait_group(K-1)`, `K = buffer.copy`) that only drains the store issued K stores ago, so it relies on same-slot stores being exactly K apart in issue order. Reuse-group members share one `accumCnt` (+1 per outer iter) and add `theIdx ∈ [0,S)`; when `S > K` consecutive iterations' staggered `(slot,phase)` ranges overlap, so the first subtile of tile t+1 reuses the slot the last subtile of tile t just stored — reuse distance **1, not K** — and `wait_group(K-1)` leaves that store in flight, so the new `local_store` overwrites the slot (or `async_tma_reduce` reads it) before the prior TMA store/reduce drained → wrong dv/dk/dq. Depth-1 (single slot, `wait_group(0)` = fully serialized) is immune, so it only surfaces once Phase 4 double-buffers the staging at the larger budget. (TLX uses `slice_id % DQ_REDUCE_STAGES`.)
- **Root cause B — Phase 3.6 marks an encoding-incompatible (unrealizable) reuse** (`WSMemoryPlanner.cpp`, `findReuseCandidate`): the dk/dv store-staging is a 128×32 subtile with **swizzle=64** (`#shared2`); the operands it could alias are 128×128 with **swizzle=128** (`#shared`). `findReuseCandidate` checked byte-fit + liveness but **not encoding**, so it marked the reuse → `computeTotalSmem` excluded the staging (looked free) → planner thought the config fit. But `mergeStagingReuseIntoHost` requires `host.getEncoding() == staging.getEncoding()` (areEncodingsCompatibleForReuse) and silently leaves the staging standalone, so the real footprint blew the HW limit. (This also means bug #8's "store-staging reused to make room" never realized for the 128-wide config — it holds purely via the strict cross-stage floor + ship-anyway backstop.)
- **Fix**: (A) `getStaggeredAccumCnt` returns the subtile index directly (`theIdx`, no accumCnt stagger; slot 0 for subtile 0) when the channel's alloc has `buffer.tmaStaging > 0`. (B) new `areReuseEncodingsCompatible` in `findReuseCandidate` rejects targets whose encoding/memory-space/element-type differ (mirrors `areEncodingsCompatibleForReuse`), so Phase 3.6 never marks an unrealizable reuse → `computeTotalSmem` stays honest → Phase 4.5 won't bump the now-counted staging past budget, and `AllocateSharedMemoryNv` packs the honestly-counted buffers under the limit. (Considered a Phase 4.5 `isAllocated` guard too, but it over-aggressively disabled *valid* compatible-encoding copy=2 reuse — e.g. idx2's intended double-buffering and `ws_memory_planner_bwd_buffer_reuse.mlir` — so it was dropped; the encoding check alone is sufficient and surgical.)
- **Tests**: Updated `ws_memory_planner_bwd_persist_early_tma.mlir` (store-staging now correctly standalone, no `allocation.reuseTarget`/`shareGroup`; q's cross-stage floor still copy=2). New Python regressions `test_bwd_early_tma_staging_depth2` and `test_bwd_early_tma_staging_depth2_persistent` (via a new `test_op(smem_budget=...)` override) force idx0 to `SMEM_BUDGET=220000` so the early-TMA store staging is double-buffered — the exact T277224987 repro, which the shipped autotuned matrix never hits (idx0/idx1 pin 200000). E2E: full shipped matrix `test_op` idx {0,1,2} × {ws, ws_persistent} HEAD_DIM=128 all pass; idx2's compatible-encoding reuse still realizes at copy=2. idx0@220000 now both correct and fits: `ttg.shared` ≈ 231940 B < 232448 HW limit, with the dk/dv store-staging allocated **standalone** (0 reuseTarget/reinterpret — no staging↔operand space reuse). WarpSpecialization lit suite: 0 failures (90 pass / 11 xfail).
- **Lit test** (root cause A): `ws_code_partition_bwd_staging_slot_rotation.mlir` checks the same-partition dv/dk staging slot is `theIdx % K` reduced (`divui/muli/subi`) from a **constant** subtile index in the staging task (`async_task_id=3`), indexing the `2x128x64xf16` reuse view with `can_rotate_by_buffer_count = 2` — versus the loop-carried m/Di buffers, which reduce the `%arg` accumCnt iter_arg. Root cause B (encoding-incompatible reuse) is covered by `ws_memory_planner_bwd_persist_early_tma.mlir`.
- **Follow-up** (T277449158): reclaiming the operand SMEM for the *mismatched*-swizzle case (128x32 sw64 staging vs 128x128 sw128 operand) is deferred — neutral/`swizzle=0` backing + per-view reinterpret. `memdesc_reinterpret` cannot convert swizzle (verifier preserves the source encoding), so the current host-typed backing can't express it; and the host↔staging non-overlap is enforced by the bug #9 reuse token, not static liveness, so `AllocateSharedMemoryNv` can't reclaim it on its own. Teaching the allocator about token-enforced disjoint live ranges is explicitly *not* the chosen direction. Full design notes live in T277449158.

### 11. FA-fwd persistent deadlock: bug #10's `getStaggeredAccumCnt` change over-applied to cross-partition staging (2026-06-28, fixed)
- **Symptom**: Runtime **deadlock** (GPU spins at 100%, `cudaStreamSynchronize` never returns) in `_attn_fwd_persist` (FA forward, `ws_persistent`, `fused_attention_ws_device_tma_dp`, non-causal, `cuda:100`/GB200). Compile+launch finishes in ~12s, then the host hangs in `synchronize`/`assert_close`. Introduced by bug #10's commit `cb9d56313`; the parent `27e57b931` is clean (bisected: HEAD~1 passes 4/4, HEAD hangs 3/3). The DP fwd-persistent path *is* covered — `test_correctness_autows.py::test_autows_fa_dp_non_causal` (`ws_persistent`, non-causal, Blackwell-gated, listed in `run_all.sh`) was already present at `cb9d56313` and hangs there. The regression escaped not from missing coverage but because that Blackwell-only pytest was not exercised on a GB200 when the bwd-focused #10 landed (its validation was the bwd matrix). Running `test_correctness_autows.py` on a GB200 reproduces the hang and is the regression guard.
- **Root cause** (`CodePartitionUtility.cpp`, `getStaggeredAccumCnt`): bug #10 made the function return the bare subtile index `theIdx` for **every** `buffer.tmaStaging > 0` alloc. That is correct only for **same-partition** staging drained by `cp.async.bulk.wait_group(K-1)` (bwd `dk`/`dv`/`dq`: producer `local_store` and consumer `async_tma_copy`/`async_tma_reduce` are the same warp task). The FA-fwd output staging `desc_o` is *also* `tmaStaging=1` but is **cross-partition**: produced in the compute task (task 0, `local_store`+`arrive(full)`) and TMA-stored in the epilogue-store task (task 2, `wait(full)`→`async_tma_copy`→`arrive(empty)`) — a producer/consumer mbarrier channel. `getBufferIdxAndPhase` derives BOTH the slot AND the empty/full mbarrier phase from the returned count; collapsing it to the constant `theIdx` pins the phase (`wait_barrier ..., %c1_i32`) so it stops flipping across persistent tiles → the producer's empty-wait is never satisfied on tile 1's slot reuse → deadlock. (For `desc_o`, S=K=2 so the *slots* came out right by coincidence; only the phase broke.)
- **Fix**: Gate the bare-`theIdx` path on **same-partition** staging — `isSameTaskStaging = isTmaStaging && getAsyncTaskIds(ch->getSrcOp()) == getAsyncTaskIds(ch->getDstOp())`. Same-partition (wait_group ring) → `theIdx`; cross-partition (mbarrier) → continuous `accumCnt (+theIdx)`, restoring the pre-#10 rotation for `desc_o`. Companion **defensive** change in `WSMemoryPlanner.cpp` `increaseFusedEpilogueCopies`: the same-partition `theIdx % K` rotation is only correct when **K | S** (S = fused subtile count); Phase 4.5 now only bumps a same-partition staging group's copies to depths that divide S. No-op for all shipped configs (`num_stages=2`, `S ∈ {2,4}`) — it's a backstop against a future config silently violating the invariant. Cross-partition staging is exempt (continuous-accumCnt rotation tolerates any K).
- **Tests**: fwd persistent now correct — `test_correctness_autows.py::test_autows_fa_dp_non_causal` passes (was a hang). All 4 bug #9/#10 bwd regressions still pass (`test_bwd_tmem_dsT_reuse_3group{,_persistent}`, `test_bwd_early_tma_staging_depth2{,_persistent}`). WarpSpecialization lit suite: 90 pass / 11 xfail, 0 unexpected failures (unchanged). Docs: `AccumulationCounters.md` (same- vs cross-partition staging table), `TMAStoreWaitPipeline.md` (`K | S` cap).
- **Lit test**: the same-partition (`theIdx`-slot) side of the gate is pinned by `ws_code_partition_bwd_staging_slot_rotation.mlir` (see #10). The cross-partition `desc_o` side (continuous-accumCnt rotation) remains covered E2E by `test_autows_fa_dp_non_causal`; a dedicated fwd-`desc_o` lit checking the rotating empty/full phase is a possible follow-up.
- **Key insight**: `buffer.tmaStaging` alone cannot distinguish the two staging kinds — both fwd `desc_o` and bwd `dk/dv` feed `AsyncTMACopyLocalToGlobalOp` and are tagged `tmaStaging=1`. The discriminator is the producer/consumer **task topology** (same vs cross partition), which selects the drain mechanism (wait_group ring vs mbarrier handshake) and hence the slot/phase formula.

### 12. reduce_dq store token-wait misrouted to epilogue → duplicated async_tma_reduce (double-reduce dq) (2026-07-11, fixed)
- **Symptom**: Wrong `dq` gradient (max-abs error ~89 at BN=64; BN=128 correct on launch 0 but wrong from launch 1) in the 2-KV HSTU cross-attention backward (`_hstu_attn_bwd_redq_2kv`, `test_cross_attention_bwd_autows_2kv`) once the 2-KV inner loop is warp-specialized. racecheck/synccheck clean; deterministic; pollution-robust once fixed. T279388065.
- **Root cause** (`PartitionSchedulingMeta.cpp`): the dq store uses TMA-atomic `async_tma_reduce` (`store_reduce="add"`), categorized `TMAReduction` → reduction partition (0). Its completion `async_tma_store_token_wait` consumes the reduce's async token, but `categorizeEpilogueStores` lumped every `TMAStoreTokenWaitOp` into `EpilogueStore`, and the epilogue scheduling walk (`getInitialSchedule`) pulled it into the epilogue-store partition (2, alongside the dk/dv store waits). With the reduce in partition 0 and its wait in partition 2, `doCodePartitionPost` **cloned** the reduce into partition 2 to satisfy the cross-partition token use → **two** `async_tma_reduce` ops → dq reduced into global twice → garbage. The user-visible tell (ttgir2tlx): the same `tma_reduce` appears in two partitions, one standalone (reduction) and one grouped with the `tma_store`s (epilogue).
- **Fix**: `isReduceTokenWait(op)` — a `TMAStoreTokenWaitOp` whose token comes from `AsyncTMAReduceOp`/`DescriptorReduceOp`. `categorizeEpilogueStores` categorizes such waits `TMAReduction` (co-locating them with their reduce in the reduction partition), and the epilogue scheduling walk skips them (`isEpilogueStoreOp(op) && !isReduceTokenWait(op)`). Phase 5's TMAReduction scheduling then places the wait in the reduction partition next to its reduce; no clone, one reduce.
- **Lit test**: `partition-scheduling-meta-reduce-dq-2kv-token-wait.mlir` (real pre-PSM 2-KV IR) — the reduce and its token-wait share partition 0 (`CHECK`/`CHECK-NEXT`), exactly one `async_tma_reduce` survives (`CHECK-NOT`), and the dk/dv store waits stay in the distinct epilogue partition 2. Verified discriminating: mutating the reduce wait to partition 2 makes FileCheck fail at the `CHECK-NEXT`.
- **Regression**: `test_cross_attention_bwd_autows_2kv` 4/4 (BN=64 and BN=128 all launches ~1.9e-3, was 89); full cross-attn bwd suite 18 pass / 6 xfail; `test_correctness_autows.py` 72 pass / 24 skip; WarpSpecialization lit suite 113 pass / 11 xfail, 0 unexpected.
- **Key insight**: a TMA store token-wait must always be co-located with the store/reduce whose token it consumes. `async_tma_reduce` shares the `async_tma_store_token_wait` op with plain TMA stores, so categorization must inspect the waited-on token's defining op, not just the wait's op type.

### 13. reorderEpilogOps moves a loop-body terminator → dq epilogue stranded after scf.yield (2026-07-14, fixed; T279622075)
- **Symptom**: `Block::getTerminator()` assert `mightHaveTerminator()` failed (`Block.cpp:250`) crashing `NVGPUWarpSpecialization` at `doMemoryPlanner` → `MemoryPlannerTmem::run` → `hasLoopCarriedAccToken` (`CodePartitionUtility.cpp:78`, `forOp.getBody()->getTerminator()`). Hit when enabling autoWS on the HSTU **self-attn backward** (`_hstu_attn_bwd`, config num_warps=8/BLOCK_M=64/BLOCK_N=64/num_stages=1, DP=1) on GB200.
- **Root cause** (`WSCodePartition.cpp`, `reorderEpilogOps`, called from `doBufferAllocation` Step 2): the pass classifies **any block containing a `tt.store`/`descriptor_store`** as an "epilog block" and collects `epilogOps` = the block tail back to the last `scf.for`/`scf.if`. The self-attn bwd warp-specializes a loop body that BOTH carries values (an `scf.yield` with the loop-carried MMA accumulator tokens) AND contains an **in-loop dq read-modify-write** (`tt.load` global dq + `tt.dot(trans(k),dqk)` + `tt.store` global dq — the plain kernel's `acc_dq`). The block terminator (`scf.yield`) was inserted into `epilogOps`; then the channel forward-slice "streamlining" ran `depOp->moveAfter(lastInBlockOperand(depOp))` on the **yield itself**, relocating it to just after its last operand-producer (the dq `tmem_load`). That stranded the dq epilogue (`mulf → load → add → truncf → store`) AFTER the terminator → ops-after-terminator (invalid IR) → the later `getTerminator` assert. Distinguishing trigger: a value-carrying loop body with an in-loop store; the intended target (post-loop dv/dk TMA-store epilogue) lives in a no-iter-arg outer-loop body and is unaffected.
- **Fix**: never treat a block terminator as a reorderable epilog op — skip `op.hasTrait<OpTrait::IsTerminator>()` when building `epilogOps`. A terminator must stay last; excluding it is strictly correct (store buckets / channelOps never referenced it). No behavior change for the intended post-loop epilogue reordering.
- **Lit test**: covered by `ws_buffer_allocation_inloop_store_terminator.mlir` (added with the fix in D111818256): `reorderEpilogOps` must not move a block terminator, else the loop body is left without a valid terminator (invalid IR that later crashes `hasLoopCarriedAccToken`).
- **Regression**: `test_self_attention_autows.py` 2/2 (fwd+bwd autoWS grads vs torch); WarpSpecialization lit suite 111 pass / 11 xfail / 0 unexpected.

### 14. Inner K loop not rescheduled under a warp-specialized outer scf.while (2026-07-20, fixed)
- **Symptom**: Unified dynamic outer-while AutoWS emits `'arith.divui' op not assigned a pipeline stage` twice on Blackwell. Compilation continues and numerical correctness passes, but the inner K-loop software pipeliner bails instead of completing the partial post-WS schedule.
- **Root cause** (`ScheduleLoops.cpp`): `doLoopSchedulePreprocessing` only recognized an outer `scf.for`. After code partition physically specializes a dynamic outer `scf.while`, its nested K loops retain `tt.scheduled_max_stage` and partial `loop.stage` assignments but never regain the `tt.warp_specialize` marker required by `getInitialSchedule` to deserialize and complete that schedule.
- **Fix**: Recognize annotated outer `scf.while` operations during post-WS preprocessing and apply `tt.warp_specialize` to their already-staged innermost `scf.for` loops. The outer while remains physically warp-specialized but is not software-pipelined.
- **Tests**: `ws_atomic_broadcast_from_psm.mlir` covers the production PSM-to-code-partition path and nested-loop rescheduling; the unified `DynamicPersistent1DScheduler` tutorial09 case checks Blackwell correctness and rejects the missing-stage diagnostic.

### 15. Unified CLC outer while silently drops AutoWS (2026-07-20, fixed)
- **Symptom**: `ClcTileScheduler` with `warp_specialize=True` on the outer condition compiles correctly but emits no `ttg.warp_specialize`; the same unified kernel specializes with the dynamic atomic scheduler.
- **Root cause**: CLC lowers to an `scf.while` carry `(valid, x)` whose condition forwards only `x`. PSM required full identity forwarding and stripped the WS marker. Its dependency traversal also assumed after-argument index 0 mapped to yield slot 0, but CLC's forwarded `x` maps to yield slot 1.
- **Fix**: Accept direct, unique, non-empty, order-preserving condition subsets. Map scheduled-body arguments back through `scf.condition` to their true yield slots, and stop forward traversal for condition-only slots that have no after-region argument. Empty, reordered, duplicate, and computed forwarding still reject safely.
- **Tests**: `ws_while_loop_autows.mlir` covers the shifted CLC-shaped carry plus reordered/computed rejection. The unified tutorial09 `ClcTileScheduler` case keeps the K loop unannotated and checks physical WS, CLC lowering, clean pipeline diagnostics, and numerical correctness on Blackwell.

### 16. No-MMA reduction WS materialization: multi-partition scalar offset assertion (2026-07-23, fixed)
- **Symptom**: `attr.size() == 1 && "expected exactly 1 partition element"` assertion in `doTaskIdPropagate` (`WSTaskIdPropagate.cpp:187`) aborting `NVGPUWarpSpecialization`. Hit on any no-MMA in-register reduction kernel (RMS norm / LayerNorm / softmax-only) warp-specialized via the Meta path (`TRITON_USE_META_WS=1`), regardless of tile shape and the `merge_epilogue` / `separate_epilogue_store` flags. Seen on sm_90 and sm_100.
- **Root cause** (`WSTaskIdPropagate.cpp`, `doTaskIdPropagate`): `PartitionSchedulingMeta`'s no-MMA scalar-offset fixup gives an offset op that feeds BOTH the load and the store the **union** of their partitions — a multi-element `ttg.partition` array (e.g. `array<i32: 1, 2>`). That is intended and documented (`PartitionSchedulingMeta.md`, the no-MMA "scalar-offset fixup") and covered by `partition-scheduling-meta-no-mma-reduction.mlir`. But the consumer `doTaskIdPropagate` hard-assumed every `ttg.partition` array had exactly one element (both the min-partition walk and the convert-to-`async_task_id` walk `assert(attr.size() == 1)`), so it aborted on the union. Only the *annotation* pass had no-MMA coverage; the *materialization* path had none, so the two passes' contracts had silently diverged. MMA kernels only ever produce size-1 arrays, so they were unaffected.
- **Fix**: Iterate every id in the `ttg.partition` array in both walks — take the min over all ids, and materialize `async_task_id` as the full (normalized) id set via the `ArrayRef<AsyncTaskId>` `setAsyncTaskIds` overload. A multi-partition op (the replicated scalar offset) is then cloned per-partition by code partitioning, exactly as `PartitionSchedulingMeta.md` describes. Behavior is byte-identical for all existing single-id kernels.
- **Lit test**: `ws_task_id_propagate_no_mma_reduction.mlir` (new) drives `@rmsnorm_no_mma` and `@maxnorm_no_mma` through the FULL pipeline (`--nvgpu-partition-scheduling-meta --nvgpu-warp-specialization`); checks the 3-warp-group `["reduction", "epilogue_store", "load"]` layout and that the scalar offset is replicated into both the epilogue-store partition (task 1, feeding `async_tma_copy_local_to_global`) and the load partition (task 2, feeding `async_tma_copy_global_to_local`). Aborts without the fix → empty FileCheck input → test fails.
- **Regression**: WarpSpecialization lit suite 119 pass / 10 xfail / 0 unexpected.
- **Key insight**: a `ttg.partition` attribute is a *set*, not a scalar. `PartitionSchedulingMeta` can legitimately assign an op to more than one partition (documented for the no-MMA scalar offset; also possible via the `TMAStoreTokenWaitOp` copy path). Any consumer of `ttg.partition` must handle size ≥ 1, not size == 1.

### 17. Hopper data-partitioned TMA stores share staging slot 0 (2026-07-30, fixed)
- **Symptom**: the Hopper tutorial09 data-partitioned GEMM writes incorrect output when two epilogue tasks TMA-store different C tiles through the same descriptor. Both stores use slot 0 of one fused staging allocation.
- **Root cause** (`WSMemoryPlanner.cpp`, `fuseEpilogueWSBuffers`): Phase 3.5 keyed TMA staging fusion on `(descriptor, originalLoad)`. Hopper register accumulators do not trace to a `ttng.tmem_load`, so both `originalLoad` values were null and the key collapsed to the descriptor alone. `doCodePartition` then merged the two task-local allocations even though each task independently indexed its staging channel at slot 0.
- **Fix**: when `originalLoad` is unavailable, include the channel's producer task in the staging-fusion key. Same-task subtiles still fuse, but concurrent data partitions targeting the same descriptor receive distinct `buffer.id`s.
- **Tests**: `ws_memory_planner_tma_store_staging_cap.mlir` checks distinct IDs for two null-origin producer tasks. `test_hopper_matmul_tma_warp_specialize[False-True-2-True-False-4-3-64-128-128-8192-8192-1024]` passes and its final TTGIR contains two one-copy staging allocations instead of one two-copy reuse group.

### 18. Self-attention bwd CLC OOM from disabled staging reuse and retained subtile allocs (2026-07-30, fixed)
- **Symptom**: HSTU self-attention CLC backward exceeds Blackwell's shared-memory limit (`Required: 280180` for the default path and `263844` with dK/dV subtiling, limit `232448`). The memory planner reports compatible dK/dV TMA store-staging and operand buffers, but Phase 3.6 selects no reuse target when all consumers lack `loop.stage`. With subtiling enabled, the planner later accounts the fused staging groups as reused while final codegen still charges their old allocations.
- **Root cause A** (`WSMemoryPlanner.cpp`, `findReuseCandidate`): consumers outside the pipelined inner loop have unknown order (`-1`, mapped to `INT_MAX`). The best-order sentinel is also `INT_MAX`, and the selector only accepts a strictly smaller order, so it never seeds `best`. The staging buffers therefore cannot reuse dead operand SMEM. Applying the fallback to every unknown-order buffer is unsafe because it creates unsynchronized ordinary-buffer reuse chains.
- **Root cause B** (`WSCodePartition.cpp`, `mergeStagingReuseIntoHost`): after rewiring staging uses to `memdesc_reinterpret` views of a shared backing allocation, the pass deliberately retained the old zero-use `local_alloc`s for later DCE. `lowerMultiTaskSubtiledRegions` can preserve these explicit allocations, so `AllocateSharedMemoryNv` charges both the shared backing and the supposedly reused subtiled staging allocations.
- **Fix**: seed an unknown-order reuse search only for TMA staging candidates; their `allocation.reuseTarget` synchronization is supplied by the existing persistent cross-tile WAR edge. After realizing reuse, erase the obsolete staging alloc immediately. `mergeStagingReuseIntoHost` runs after the final channel-consuming transformation, so stale `Channel::allocOp` pointers are no longer observed.
- **Tests**: `ws_memory_planner_bwd_buffer_reuse.mlir` covers compatible dK/dV staging selection onto distinct operand targets. `ws_code_partition_bwd_staging_slot_rotation.mlir` now checks the two subtiled staging buffers are `memdesc_reinterpret` views and that no `buffer.tmaStaging = 1` `local_alloc` survives. HSTU self-attention CLC backward passes both the default and `HSTU_SELF_BWD_DKDV_SUBTILE=2` paths with relative-L2 dq/dk/dv `2.81e-3 / 2.35e-3 / 2.35e-3`.

### 19. Block-scale descriptor load lands its TMA copy in a non-NVMMA buffer (2026-08-16, fixed)
- **Symptom**: `'ttng.async_tma_copy_global_to_local' op TMA descriptor must have NVMMA shared layout` at compile time in all three `test_autows_quantized_matmul_tma[mxfp4/nvfp4/mxfp8]` cases. Bisected to `62df80353` ("[AutoWS] Convert tt.descriptor_load to nvws.descriptor_load", #2267); its parent is clean.
- **Root cause** (`WSLowerMem.cpp`, `doConvertDescriptorLoadsToNVWS`): the conversion reuses the sole consumer's `local_alloc`/`local_store` buffer as the TMA landing buffer. For a block-scale load that buffer carries `#ttg.shared_linear` — the layout its `memdesc_reshape`/`memdesc_trans` chain needs to feed `tmem_copy` — which `verifyTMAEncoding` rejects. Upstream's `lowerTMALoad` never reuses a consumer buffer: it always allocates from `getEncodingFromDescriptor` and then `replaceUsesWithLocalLoad`.
- **Fix**: new `isValidTMADestEncoding` (mirrors `verifyTMAEncoding`: NVMMA shared, untransposed, and matching transposed / swizzle / element bit width / fp4Padded once the descriptor carries an encoding) gates both reuse paths. On failure the load falls through to the function's existing fresh-NVMMA-buffer + `local_load` path.
- **Tests**: `test_autows_quantized_matmul.py` 3 passed (was 3 failed). A dedicated lit test is still missing.

### 20. reorderEpilogOps hoists an SMEM consumer above its nvws.descriptor_load producer (2026-08-16, fixed)
- **Symptom**: `WSCodePartition.cpp: Assertion 'isBackwardOfChannelLoop(masterChannel)' failed` in `insertAsyncComm` compiling `_hstu_attn_bwd` (`test_self_attention_fwd_autows[256-4]` / `[512-2]`, which exercise the backward kernel). Same bisect point as #19.
- **Root cause** (`WSCodePartition.cpp`, `reorderEpilogOps`): the streamlining step moves each epilogue op right after its last **SSA** operand. Before #2267 the dv MMA reached its `do` producer through SSA (`tt.descriptor_load` → `local_alloc` → MMA), so it could never be hoisted above it. `nvws.descriptor_load` writes SMEM and returns nothing, so that edge became a *memory* dependence the streamliner cannot see, and the MMA was hoisted above its own producer (block positions 130 vs 140 on a `buffer.copy=1` alloc). The assert only ever covered the operand-D TMEM forward/backward pair, so a consumer-before-producer SMEM channel trips it.
- **Fix**: refuse a hoist that would cross another op touching one of the same underlying buffers (root traced through `memdesc_index`/`trans`/`reshape`/`reinterpret`/`subslice`). Sinking, and ops with no memdesc operand, are unaffected. **The same-buffer restriction is load-bearing**: a first attempt that blocked hoisting across *any* non-pure op compiled fine but produced a runtime `misaligned address`.
- **Tests**: `test_self_attention_autows.py` 7 passed (was 2 failed / 5 passed). A dedicated lit test is still missing.

### 21. Copy-safety floor pushes the plan over budget, so Phase 3.6 aliases live operands (2026-08-16, fixed)
- **Symptom**: wrong `addmm` results (~49.5% of elements off, max abs diff ~630-710) in the four `test_autows_addmm_tma_persistent[True-2-*-*-2-False-4-3-64-128-256-1024-1024-8192]` cases (`generate_subtiled_region=True`, DP=2, `EPILOGUE_SUBTILE=2`, `BLOCK_M=256`). Bisected to `d7d0a955a` ("[store-wait] Reserve FA backward staging with static copy safety", #2834); `11938a5ba` is clean.
- **Root cause** (`WSMemoryPlanner.cpp`): `getStaticSmemCopySafetyFloor` floors every TMA operand of a DP MMAv5 loop at the configured depth, which for this config makes the base plan 229376 B against a 229364 B budget — **12 bytes over**. Phase 3.6 then pays for those 12 bytes by aliasing whole 16 KB buffers onto live operands: the four fused `c_desc_staging` subtiles onto `a_0`, and the bias buffers onto `a_1`/`b`. Only *staging* reuse gets the Step 7.5 WAR token (see #9), so the bias aliasing has no guard at all and the epilogue scribbles on operands the inner loop is still reading. The pre-#2834 planner never got there: it left `b` at copy=2 and fit.
- **Fix**: two parts. (1) `findReuseCandidate` now applies the co-live filter to the **target** side as well — only a TMA-staging candidate may land on a host that `isSmemLiveAcrossInnerLoop`, mirroring the candidate-side filter Phase 3.6 already had. (2) New `relaxCopySafetyFloorToBudget` gives the copy-safety floor back — it is a schedule proof, unlike the cross-stage floor, which stays unconditional — one copy at a time, largest buffer first, until the plan fits; Phase 4 then re-bumps whatever the budget allows. Relaxing straight back to the pre-floor depth is **not** enough: Phase 3.7 spends the freed slot on staging and Phase 4 can no longer restore the operand.
- **Tests**: `test_autows_addmm.py` 73 passed (was 4 failed / 69 passed); the resulting plan is 4=2 / 5=3 / 6=3 with no `allocation.reuseTarget`. A dedicated lit test is still missing.

### 22. Unlowered `nvws.descriptor_load` on a kernel with no partitions (2026-08-18, fixed)
- **Symptom**: `error: LLVM Translation failed for operation: builtin.unrealized_conversion_cast` → `RuntimeError: failed to translate module to LLVM IR`, reported against the source line of a TMA load. Hit on tritonbench `bmm`'s `triton_ws_persistent_tma_bmm` backend (fp32, H100, triton-beta); the identical kernel with `WARP_SPEC=False` compiles.
- **Manifestation**: the final IR still contains `nvws.descriptor_load` with Triton types (`!tt.tensordesc`, `!ttg.memdesc`) and **no** `ttg.warp_specialize` op anywhere. The reported casts are the four backward `unrealized_conversion_cast`s (`!llvm.struct<(ptr<3>, i32, i32)>` → `!ttg.memdesc`, `!llvm.ptr` → `!tt.tensordesc`) materialized to keep that unconverted op verifying; `ReconcileUnrealizedCastsPass` cannot fold them because their consumer was never lowered. The cast is the symptom, the stranded NVWS op is the defect.
- **Root cause** (`WarpSpecialization.cpp`, `runOnFuncOp`): `doConvertDescriptorLoadsToNVWS` (added by D112909940) rewrites **every** `tt.descriptor_load` in the function unconditionally, and `optimizeTMALoads` — called from `insertAsyncComm` during `doCodePartition` — is the only thing that ever erases one. There is no rollback path. `MemoryLowering.md` states the assumed invariant ("runs ... after AutoWS has passed its final eligibility bailout") and `BufferAllocation.md` states the consequence ("No unconverted descriptor load is allowed past this point"), but no gate enforced it. When PartitionSchedulingMeta assigns no partitions, code partitioning creates no channels, `optimizeTMALoads` never runs, and the converted op survives to LLVM translation. PSM assigns nothing here because the `tt.dot` is an IEEE fp32 dot (`ALLOW_TF32=False`) that lowers to FMA, not wgmma/tcgen5 — accumulator is `!llvm.struct<(f32, f32)>` — so there is no MMA to build producer/consumer roles around.
- **Fix**: a no-partition gate right after `doTaskIdPropagate` and before any rewriting — `if (getNestedAsyncTaskIds(funcOp).empty()) return bailOut(funcOp);`. Deliberately phrased as "no partitions" and **not** "no MMA": the no-MMA reduction kernels of bug #16 do get partitions from PSM's no-MMA path and must keep specializing. Also deliberately `empty()` and not `size() < 2` — a single-partition function still legitimately runs TMA store-wait annotation (`ws_tma_store_pipelining_option.mlir` regresses on the stricter form).
- **Lit test**: `ws-skip-no-partitions-descriptor-load.mlir` (new) — a `tt.warp_specialize`-marked loop with a `tt.descriptor_load` and no partitions; checks the load stays unconverted and no `nvws.descriptor_load`/`ttg.warp_specialize` appears. Verified discriminating: without the gate the NVWS op appears and FileCheck fails.
- **Companion**: `ws_code_partition_empty_producer_guard.mlir` now runs `--nvgpu-partition-scheduling-meta` before `--nvgpu-warp-specialization`. Its raw kernel carries no `ttg.partition`, so under the new gate the pass rejected it early and the test silently stopped covering bug #7 — while its pre-fix output had been pinning 4 unlowered `nvws.descriptor_load` ops and no `ttg.warp_specialize`. With PSM in the pipeline it gets real partitions, emits physical WS regions, and reaches `createChannelPost` as intended.
- **Key insight**: `tt.warp_specialize` on a loop is a *request*, not a decision. Any pass that rewrites ops on the strength of that marker must first confirm partitions exist, because the pass that undoes the rewrite only runs when they do.

## Debugging Workflow
- `t.dump` captures IR after each WarpSpec pass (doTaskIdPropagate → doBufferAllocation → doMemoryPlanner → doCodePartition → ...)
- IR after PartitionSchedulingMeta uses `ttg.partition = array<i32: N>` attributes (not `async_task_id`)
- IR after doTaskIdPropagate converts `ttg.partition` to `async_task_id` annotations
- To check partition assignments: look at IR between `NVGPUPartitionSchedulingMeta` and `NVGPUWarpSpecialization` dump sections
- Build: see xxx/build-triton.txt
- To run a single pass: `triton-opt --nvgpu-partition-scheduling-meta="merge-epilogue-to-computation=true" input.mlir`
- To enable debug: add `-debug-only=tritongpu-partition-scheduling`
- To add stack traces on specific ops: instrument `setPartition()` in `lib/Dialect/TritonGPU/Transforms/WarpSpecialization/Partition.cpp`

## Key Concepts
- `PartitionSchedulingMeta` assigns `ttg.partition` attributes → `doTaskIdPropagate` converts to `async_task_id`
- Pointer-typed tensors (`!tt.ptr<T>`) should not be cross-partition
